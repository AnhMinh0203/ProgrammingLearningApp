<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProgramingLearningApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.lesson.lessionUser"
            android:exported="false" />
        <activity
            android:name=".ui.courseManagement.CourseManager_Update"
            android:exported="false"
            android:theme="@style/Theme.ProgramingLearningApp.NoActionBar" />
        <activity
            android:name=".ui.lessonManagement.LessonManagement_Update"
            android:exported="false"
            android:theme="@style/Theme.ProgramingLearningApp.NoActionBar" />
        <activity
            android:name=".ui.lesson.lessonDetail"
            android:exported="false"
            android:theme="@style/Theme.ProgramingLearningApp.NoActionBar" />
        <activity
            android:name=".ui.lessonManagement.LessonManagement_Create"
            android:exported="false"
            android:theme="@style/Theme.ProgramingLearningApp.NoActionBar" />
        <activity
            android:name=".ui.course.courseDetail"
            android:exported="false"
            android:theme="@style/Theme.ProgramingLearningApp.NoActionBar" /> <!-- <activity -->
        <!-- android:name=".ui.my_courses.My_courses" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".ui.register_course.test1" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".ui.courseManagement.CourseManager_Create"
            android:exported="false"
            android:theme="@style/Theme.ProgramingLearningApp.NoActionBar" />
        <activity
            android:name=".ui.MemberManager.MemberManagementEdit"
            android:exported="false" />
        <activity
            android:name=".ui.MemberManager.MemberManagement"
            android:exported="true"
            android:theme="@style/Theme.ProgramingLearningApp.NoActionBar" />
        <activity
            android:name=".ui.auth.Register"
            android:exported="true"
            android:theme="@style/Theme.ProgramingLearningApp.NoActionBar" />
        <activity
            android:name=".ui.auth.Authentication"
            android:exported="true"
            android:theme="@style/Theme.ProgramingLearningApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ProgramingLearningApp.NoActionBar" />
    </application>

</manifest>